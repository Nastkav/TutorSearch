@model Web.Models.TutorProfile.DetailsViewModel

@{
    ViewBag.Title = "Edit Tutor Profile";
    Layout = "_Layout";
}
<div class="row">
    <div class="row m-3"></div>
    <h1 class="text-center">@ViewData["Title"]</h1>
</div>
<partial name="EditForm" />

<!-- Calendar -->
<div class="card bg-light mt-3" id="schedule">
    <div class="card-body">
        <h5 class="card-title text-center">Available tutor schedule</h5>
        <div class="container">
            <div class="row justify-content-md-center">
                <div><i class="fa fa-info "></i> Зображені години, у які є можливість займатися з учнями.</div>
                <div id="calendar" data-tutorid="@Model.TutorCard.Id"></div>
                <script src="~/js/tutor-profile/edit/calendar.js"></script>
                <div class="row mt-3">
                    <button id="calendar-reload" type="submit" class="col col-3 btn btn-dark">Cancel</button>
                    <div class="col col-6"></div>
                    <button id="calendar-submit" type="submit" class="col col-3 btn btn-primary">Update</button>
                </div>
            </div>
        </div>
    </div>
</div>
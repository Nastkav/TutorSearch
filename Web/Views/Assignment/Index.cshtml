@model Web.Models.Assignments.AssignmentListVm

@{
    ViewData["Title"] = "Вчитель: Завдання";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Додати завдання</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.First().Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.First().Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.First().SubjectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.First().CreatedAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.First().Deadline)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Assignments)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deadline)
            </td>
            <td>
                @*TODO <a asp-action="Details" asp-route-id="@item.Id">Details</a> | *@
            </td>
        </tr>
    }
    </tbody>
</table>